NODE_ENV=development
PORT=4000
CORS_ORIGIN=http://localhost:5173

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# AI providers — every capability degrades gracefully (feature returns
# "unavailable" rather than crashing) when its key is unset.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
ELEVENLABS_API_KEY=
